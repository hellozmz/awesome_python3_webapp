
    客户端（浏览器）发起请求
    路由分发请求（这个框架自动帮处理），add_routes函数就是注册路由。
    中间件预前处理
        打印日志
        验证用户登陆
        收集Request（请求）的数据
    RequestHandler清理参数并调用控制器，Django和Flask把这些处理请求的函数称为view functions
    控制器做相关的逻辑判断，有必要时通过ORM框架处理Model的事务。
    模型层的主要事务是数据库的查增改删。
    控制器再次接管控制权，返回相应的数据。
    Response_factory根据控制器传过来的数据产生不同的响应。
    客户端（浏览器）接收到来自服务器的响应。

一次HTTP请求的生命周期大概就是这样子了，后端的处理过程就是这流程，前端的我有时间再画。本人的博客就是基于这里的实战项目教程，目前还在运行中，源代码在这里，欢迎勾搭调戏，再次谢谢廖老师写的精彩教程，虽然时间久了有坑，但瑕不掩瑜。